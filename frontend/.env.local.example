# Variables d'environnement Frontend (copiez en .env.local pour le dev local)

# En local : laissez vide ou commenté — le proxy Vite redirige /api vers localhost:5000
# En production (Render) : mettez l'URL du backend
VITE_API_URL=
